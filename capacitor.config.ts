import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'bi.hogi.imagecaption',
  appName: 'gestion-conge-mobile',
  webDir: 'public',
  server: {
    androidScheme: 'https'
  }
};

export default config;
